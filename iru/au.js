let letter = 'x';

switch (letter) {
  case 'a':
    // Code to execute if letter is 'a'
    break;
  case 'b':
    // Code to execute if letter is 'b'
    break;
  case 'x':
    // Code to execute if letter is 'x'
    break;
  default:
    // Code to execute if letter doesn't match any case
}
